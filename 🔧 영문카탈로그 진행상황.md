# 🔧 영문 카탈로그 진행상황

## 무엇을 하는 중인가
기존 한글 카탈로그(https://suchang-marketing.github.io/catalog/)의 **영문판**을 별도로 만든다.
- 한글판 `index.html` = 유지 (내용은 그대로, 언어 전환 단추만 추가)
- 영문판 = **`en/index.html`** → 주소 https://suchang-marketing.github.io/catalog/en/

## 결정한 것 (2026-08-25)
- 이름 표기 = **영문명 + 학명(라틴어) 병기**. 해조류·약재는 영어 일반명이 없는 게 많아 학명이 국제 공용어 역할.
- 학명은 종(種)까지 확신할 때만 종명을 적고, 아니면 **속명 + sp.** 로 두고 `종명확인` 표시. 틀린 학명보다 안전.
- 구글 원본 마스터 시트에도 영문명 열 추가 → **커넥터로는 셀을 못 씀**(읽기·새 파일 생성만 가능). 붙여넣기용 표를 만들어 우경님이 한 열 붙이는 방식.
- 한글판의 부위 깨짐 버그도 같이 고치기로 함(우경님 승인).

## 완료한 것
- [x] 저장소 클론 → `D:\CLAUDE\catalog-work`
- [x] **237종 영문명+학명 대조표 `en-names.csv`** (농산물89 + 해조류88 + 약재60, 빠짐 0 / 중복 0)
- [x] 가공품명 99개 `en-goods.csv` · 용어집 46개 `en-terms.csv`
- [x] **변환 스크립트 `build-en.js`** — 한글판 → 영문판 자동 생성. 한글판이 바뀌면 `node build-en.js` 다시 돌리면 됨
- [x] **영문판 `en/index.html` 완성** (1.89MB · 237종 · 사진 145장 · HS코드 그대로)
      - 이름 아래 학명 이탤릭 표시, 상세에 Scientific Name / Korean Name 줄 추가
      - **학명으로도 검색됨** (바이어가 Sargassum fusiforme 로 찾으면 Hijiki가 나옴)
      - 엑셀 내려받기에 학명·한글명 열 추가
      - 브라우저로 실제 확인 완료 (목록·상세·검색·언어전환 모두 정상)
- [x] **한글판 버그 수정** — 부위 "전초(잎,줄기,뿌리)"가 쉼표에서 잘려 세 조각으로 보이던 것 **86개 품목** 수정 (`fix-ko.js`)
- [x] 한글판·영문판에 **KO / EN 전환 단추** 추가
- [x] 원본 백업 `index.ko-backup.html` (깃에는 안 올림)
- [x] 마스터 시트 붙여넣기용 표 `마스터시트_붙여넣기용.csv` (MaterialID 순 237행)
- [x] **깃허브 올리기 완료 · 인터넷에 떴음** (2026-08-25)
      - https://suchang-marketing.github.io/catalog/ (한글)
      - https://suchang-marketing.github.io/catalog/en/ (영문)
- [x] 이 컴퓨터에 **회사 깃허브 계정(suchang-marketing) 로그인 추가** — 개인 계정(woowooff)도 같이 남아 있음.
      계정 전환은 `gh auth switch`. ⚠️ 이건 **이 컴퓨터**에 붙은 설정 → 기기 바꾸면 다시 해야 함.
      (`gh auth login`은 브라우저 인증을 기다려야 해서 **클로드 창 안에서는 시간초과로 실패함** → 별도 PowerShell 창을 띄워서 해야 함)
- [x] 언어전환 단추가 영문판에 두 벌 나오던 버그 수정 (한글판에 이미 단추가 생긴 뒤 빌드하면 중복됨 → 이제 바꿔 끼움)

- [x] **영문판 알파벳순 정렬** (2026-08-25) — 한글판처럼 농산물→해조류→약재 순, 그 안에서 **영문명 A~Z**. `build-en.js`의 `CAT_ORDER` 부분

## 다음에 할 것
- [ ] 구글 마스터 시트에 영문명 열 붙이기 (`마스터시트_붙여넣기용.csv` 사용)
      · 2026-08-25 시도: **구글 커넥터로는 셀을 못 씀**(제목·폴더만 수정 가능) → 브라우저 조작으로 방향 전환했으나 인터넷이 끊겨 중단. 시트는 **아무것도 안 고쳤음**
      · 백업 사본 떠둠: 구글드라이브 `원료master_최종_현행 (백업 2026-08-25 영문명작업 전)`
      · 시트 URL: https://docs.google.com/spreadsheets/d/1GAEio6aATAR_-4bYrHafUu3d6bFyc1noncmG-v41aaE/edit?gid=1764555240
- [ ] `종명확인` 57건 검토 — 대부분 해조류. 급하지 않음. `en-names.csv`만 고치고 `node build-en.js` 다시 돌리면 반영됨
- [ ] (선택) 영문 PDF — 지금 영문판의 PDF 단추는 **한글 PDF**를 가리킴 (`PDF (KR)`로 표시해 둠)

## 다시 만들 때 (어떤 파일을 고치면 되나)
| 고치고 싶은 것 | 고칠 파일 |
|---|---|
| 원물 영문명·학명 | `en-names.csv` |
| 가공품 영문명 | `en-goods.csv` |
| 분류·가공·부위 등 용어 | `en-terms.csv` |
| 화면 문구(제목·꼬리말 등) | `build-en.js` 안의 REPL_HEAD / REPL_TAIL |

→ 고친 뒤 `node build-en.js` 한 번이면 영문판이 새로 만들어짐.

## 원본 위치
- 마스터 시트: 구글드라이브 `원료master_최종_현행` (ID 1GAEio6aATAR_-4bYrHafUu3d6bFyc1noncmG-v41aaE)
- 깃허브: suchang-marketing/catalog (공개 저장소)
